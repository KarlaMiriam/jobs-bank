services:
  - type: web
    name: jobs-bank
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn api:app --host 0.0.0.0 --port $PORT"
    region: oregon
    plan: free
    envVars:
      - key: PYTHONPATH
        value: .
